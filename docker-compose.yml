version: '3.4'

services:
  web:
    build: .
    depends_on:
      - database
    ports:
      - "8000:8000"
    environment:
      DB_HOST: database
      DB_PORT: 3306

  database:
    build: ./db
    ports:
      - "3306:3306"
    volumes:
      - "db-data:/var/lib/mysql"

volumes:
  db-data:
